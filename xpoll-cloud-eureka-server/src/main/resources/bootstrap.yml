spring:
  application:
    name: xpoll-cloud-eureka-server
  cloud:
    config:
      uri: http://localhost:8781
management:
  endpoints:
    web:
      exposure:
        include: '*'
        
eureka:
  server:
    peer-eureka-nodes-update-interval-ms: 10000